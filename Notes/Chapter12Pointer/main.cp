#include <iostream>
#include "dlist.h"
using namespace std;

int main()
{
   DList d;
   cout << "Initial list:\n";
   d.Print();

   d.Insert(5.4);
   d.Insert(2.3);
   d.Insert(10.67);

   double * p;
   //&p address of p
   //content of p
   //*p content of the memory location
   p = ar

   cout << *p << endl;

   //cout << d.GetElement(1);




}
